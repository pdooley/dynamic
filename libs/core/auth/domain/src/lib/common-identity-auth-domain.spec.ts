import { commonIdentityAuthDomain } from './common-identity-auth-domain';

describe('commonIdentityAuthDomain', () => {
  it('should work', () => {
    expect(commonIdentityAuthDomain()).toEqual('common-identity-auth-domain');
  });
});
