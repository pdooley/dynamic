import { coreDowntimeDomain } from './core-downtime-domain';

describe('coreDowntimeDomain', () => {
  it('should work', () => {
    expect(coreDowntimeDomain()).toEqual('core-downtime-domain');
  });
});
