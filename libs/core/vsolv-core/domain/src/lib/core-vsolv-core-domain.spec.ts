import { coreVsolvCoreDomain } from './core-vsolv-core-domain';

describe('coreVsolvCoreDomain', () => {
  it('should work', () => {
    expect(coreVsolvCoreDomain()).toEqual('core-vsolv-core-domain');
  });
});
