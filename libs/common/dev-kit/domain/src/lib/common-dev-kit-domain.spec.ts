import { commonDevKitDomain } from './common-dev-kit-domain';

describe('commonDevKitDomain', () => {
  it('should work', () => {
    expect(commonDevKitDomain()).toEqual('common-dev-kit-domain');
  });
});
